Shared skin styles and token entrypoints for `@ofoqh/ux`.

These files are packaged so product apps can import shared branding tokens
from the UX package instead of defining the root token contract locally.
